# Администрирование и автоматизация систем Linux

## 🚀 Проект: Мониторинг и автоматизация DevOps-серверов Linux
Представьте, что вы управляете **производственным сервером на базе Linux** и вам необходимо обеспечить надлежащее управление **пользователями, журналами и процессами**. Вам предстоит выполнять реальные задачи, такие как **анализ журналов, управление томами и автоматизация**, чтобы улучшить свои навыки DevOps.

---

## 📌 Задачи

### **1️⃣ Управление пользователями и группами**
- Узнайте о пользователях, группах и правах доступа в Linux. (`/etc/passwd`, `/etc/group`).
- **Задача:**  
  - Создайте пользователя `devops_user` и добавьте его в группу `devops_team`.
  - Установите пароль и предоставьте доступ **sudo**.
  - Ограничить вход по SSH для определенных пользователей в `/etc/ssh/sshd_config`.
  
---

### **2️⃣ Права доступа к файлам и каталогам**
- **Задача:**  
  - Создайте папку `/devops_workspace` и файл `project_notes.txt`.
  - Установить права доступа:
    - **Владелец может редактировать**, **группа может читать**, **другие не имеют доступа**.
  - Используйте команду `ls -l` для проверки прав доступа..

---

### **3️⃣ Анализ лог-файлов с помощью AWK, Grep и Sed**
Журналы событий играют решающую роль в DevOps! Вы будете анализировать журналы, используя файл **Linux_2k.log** из **LogHub**. ([GitHub Repo](https://github.com/logpai/loghub/blob/master/Linux/Linux_2k.log)).
- **Задача:**  
  - **Загрузите файл журнала** из репозитория.
  - **Извлечение данных с помощью команд:**
    - Используйте команду `grep`, чтобы найти все вхождения слова **"error"**.
    - Используйте `awk` для извлечения **временных меток и уровней логирования**.
    - Используйте команду `sed` для замены всех IP-адресов на **[REDACTED]** в целях безопасности.
  - **Бонус:** Найдите наиболее часто встречающуюся запись в журнале, используя `awk` или `sort | uniq -c | sort -nr | head -10`.
  
---

### **4️⃣ Управление томами и использованием диска**
- **Задача:**  
  - Создайте директорию `/mnt/devops_data`.
  - Смонтировать новый том (или loop-устройство для локальной практики).
  - Проверьте с помощью команд `df -h` и `mount | grep devops_data`.
---

### **5️⃣ Управление и мониторинг процессов**
- **Задача:**  
  - Запустите фоновый процесс (`ping google.com > ping_test.log &`).
  - Используйте `ps`, `top`, и `htop` для мониторинга.
  - Завершите процесс и убедитесь, что он завершен.

---

### **6️⃣ Автоматизация резервного копирования с помощью сценариев командной оболочки**
- **Задача:**  
  - Напишите скрипт для резервного копирования `/devops_workspace` в виде файла `backup$(date +%F).tar.gz`.
  - Сохраните его в `/backups` и запланируйте его запуск с помощью `cron`.
  - Настройте скрипт так, чтобы он выводил сообщение об успешном завершении **зеленым текстом** с помощью команды `echo -e`.

---

## 🎯 Бонусные задачи (Опционально 🚀)
1. Найдите **5 наиболее часто встречающихся сообщений в логе** файла `Linux_2k.log` с помощью команд `awk` и `sort`.
2. Используйте команду `find`, чтобы вывести список **всех файлов, измененных за последние 7 дней**.
3. Напишите скрипт, который извлекает и отображает только **сообщения об ошибках и предупреждениях** из файла `Linux_2k.log`.